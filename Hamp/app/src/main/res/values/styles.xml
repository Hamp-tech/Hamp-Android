<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="TranslucentStatusTheme" parent="AppTheme" />

    <style name="WhiteTheme" parent="AppTheme" />

    <style name="HampEditText">
        <item name="colorControlNormal">@color/black</item>
        <item name="colorControlActivated">@color/cerise_pink</item>
        <item name="colorControlHighlight">@color/cerise_pink</item>
    </style>

    <style name="HampScrollView" />

</resources>
